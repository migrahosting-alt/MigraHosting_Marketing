# MigraHosting Marketing Website - Environment Variables
# Copy this file to .env.local and fill in your actual values

# ==========================================
# Backend (Express) Variables
# ==========================================

# Server Configuration
NODE_ENV=development
PORT=4242
HOST=0.0.0.0

# mPanel API Configuration
MPANEL_API_URL=http://localhost:2271/api
MPANEL_API_KEY=your_mpanel_api_key_here

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# JWT Secret for Session Management (generate with: openssl rand -base64 32)
JWT_SECRET=your_random_jwt_secret_minimum_32_characters_here

# Database (SQLite for local dev, PostgreSQL for production)
DATABASE_URL=file:./server/data.sqlite

# Development Settings
DEV_ALLOW_UNVERIFIED_WEBHOOKS=true

# ==========================================
# Frontend (Vite) Variables (prefix with VITE_)
# ==========================================

# Backend API URL (where Express server runs)
VITE_API_BASE_URL=http://localhost:4242
VITE_API_URL=http://localhost:4242

# mPanel URLs
VITE_MPANEL_URL=http://localhost:3001
VITE_MPANEL_CONTROL_PANEL_URL=http://localhost:2271
VITE_MPANEL_API_URL=http://localhost:2271/api

# CMS API URL (for blog posts and testimonials)
VITE_CMS_API_URL=http://localhost:4243/api/cms

# Stripe Publishable Key (public, safe to expose)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# ==========================================
# Production Values (for reference)
# ==========================================

# Backend Production:
# NODE_ENV=production
# STRIPE_SECRET_KEY=sk_live_your_live_secret_here
# STRIPE_WEBHOOK_SECRET=whsec_your_live_webhook_secret_here
# MPANEL_API_URL=https://panel.migrahosting.com/api
# DATABASE_URL=postgresql://user:pass@host:5432/migrahosting
# DEV_ALLOW_UNVERIFIED_WEBHOOKS=false

# Frontend Production:
# VITE_API_BASE_URL=https://api.migrahosting.com
# VITE_MPANEL_URL=https://panel.migrahosting.com
# VITE_MPANEL_CONTROL_PANEL_URL=https://panel.migrahosting.com
# VITE_CMS_API_URL=https://cms.migrahosting.com/api/cms
# VITE_STRIPE_PUBLISHABLE_KEY=pk_live_your_live_key_here

# ==========================================
# Notes
# ==========================================
# 1. NEVER commit .env or .env.local to Git (.gitignore should block them)
# 2. Use strong random values for JWT_SECRET (32+ characters)
# 3. Stripe test keys: pk_test_* and sk_test_*
# 4. Stripe live keys: pk_live_* and sk_live_*
# 5. For local dev, all URLs point to localhost with correct ports
# 6. mPanel must be running on port 2271 for full integration
# 7. Backend API must be running on port 4242
